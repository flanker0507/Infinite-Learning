package KotlinCollection

fun main() {
    val days = arrayOf("Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday")

    val firstData = days.first()
    val latestData = days.first()

    println(firstData)
    println(latestData)
}